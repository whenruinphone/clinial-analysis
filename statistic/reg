备注：采用线性回归模型，检验连续三次的用药前血药浓度（240 mg Q2W的C2D15和C3D1和C3D15给药前浓度）的斜率与0的差异是否有统计学差异，
如果无统计学显著差异（p > 0.05），证明血药浓度到达稳态。

ods output parameterestimates=pvalue(where=(variable ne 'Intercept'));
proc reg data=tot(where=(aval ne .));
by CANCLASN ENRLID ;
model aval=avisitn;
run;


